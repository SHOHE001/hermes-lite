# hermes-lite 共通設定の雛形。
# `cp .env.example .env` してから値を埋める（.env 自体は .gitignore 済み、コミットしない）。
# bin/run-claude.sh（各 jobs/）と systemd/*.service（EnvironmentFile）の両方から読まれる。

# --- claude -p 呼び出しの既定値（jobs/<name>/job.env で上書き可能） ---
DEFAULT_MAX_TURNS=
DEFAULT_TIMEOUT_SEC=
DEFAULT_MAX_BUDGET_USD=
DEFAULT_MODEL=

# --- Discord 通知（lib/notify.sh が使う webhook） ---
DISCORD_WEBHOOK_URL=

# --- mail-watch 専用チャンネル ---
# 通知の投稿先。jobs/mail-watch/job.env が DISCORD_WEBHOOK_URL に再代入する。
# 空なら上の共通 webhook にフォールバックする（現行と同じ挙動）。
MAIL_WATCH_DISCORD_WEBHOOK_URL=
# 同じチャンネルの ID（カンマ区切りで複数可）。ここでの発言だけが
# gateway/discord/mail_rules_handler.py に流れ、通知ルール（var/mail-watch/rules.md）を
# 編集できる。空ならフィードバック機能ごと無効。INPUT_CHANNEL_IDS とは別リストにする。
MAIL_WATCH_CHANNEL_IDS=

# --- SwitchBot API v1.1（Issue #12: 家電スケジュール） ---
# 取得手順: SwitchBot アプリ > プロフィール > 設定 > App Version を連打 >
# Developer Options が出現 > Get Token / Get Secret
# lib/switchbot.py と systemd/switchbot-action@.service が参照する。
# ログ・Discord 通知には値を出さない（CLAUDE.md 規約準拠）。
SWITCHBOT_TOKEN=
SWITCHBOT_SECRET=
